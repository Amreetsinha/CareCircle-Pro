// Use empty string to make requests relative, which will be proxied by Vite
// Vite proxy config in vite.config.js handles routing to http://localhost:8080
const API_BASE_URL = "";

export default API_BASE_URL;
