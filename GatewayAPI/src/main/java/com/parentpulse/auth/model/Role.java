package com.parentpulse.auth.model;

/*
 * 
 * Defines system-level roles. 
 * These roles are used across gateway and downstream services.
 */

public enum Role {

	ROLE_PARENT, 
	ROLE_PROVIDER, 
	ROLE_ADMIN
}

